dataset:
  name: ssdd
  limit_per_split: 256
  val_fraction: 0.2
variants:
  - raw
  - bundle_a
  - bundle_a_conservative
  - bundle_b
  - bundle_d
detector:
  backend: ultralytics
  model: yolov8n.pt
  epochs: 12
  imgsz: 640
  batch: 4
  workers: 0
  eval_split: test
  device: ""
outputs:
  root: outputs/downstream_detection_medium
